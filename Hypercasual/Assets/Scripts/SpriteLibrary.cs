using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SpriteLibrary : MonoBehaviour
{
    public Sprite hBurgBun, hBurgLetuce, hBurgCheese, hBurgKetchapp, hBurgMustard,//ham burger sprites TODO: ADD FINISHED AND FAILED SPRITES
        PizzDough, PizzBestTopping, PizzPep, PizzSauce; //pizza sprites TODO: ADD FINISHED AND FAILED SPRITES

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
